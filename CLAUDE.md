# このプロジェクトについて（Claude Code 向けの案内）

**タイトル：アルカナエクスプロージョン**

トランプ3箱で遊べるオリジナルカードゲームの、PC・スマホ版。
オグさんと共同開発者の**共同開発プロジェクト**。

> 📛 タイトルを変えるときは、画面・README・起動時のバナーがバラバラにならないよう
> `web/index.html`、`web/cards.html`、`README.md`、`server/app.py` をまとめて直すこと。

---

## 📄 まず読むファイル

質問されたら、まずここを見ること。**推測で答えないこと。**

| 聞かれたこと | 見るファイル |
|---|---|
| **TODO / やること / 残件** | [`TODO.md`](TODO.md) ← **タスクの正本はここ** |
| ゲームのルール・数値 | [`docs/02_ゲームルール.md`](docs/02_ゲームルール.md) |
| カード52種の性能 | [`docs/03_カードリスト.md`](docs/03_カードリスト.md)（自動生成） |
| ソースの構造・改造方法 | [`docs/04_設計とロードマップ.md`](docs/04_設計とロードマップ.md) |
| 環境の作り方・運用ルール | [`docs/05_はじめかた（共同開発者へ）.md`](docs/05_はじめかた（共同開発者へ）.md) |
| バックアップ・git | [`docs/06_運用メモ.md`](docs/06_運用メモ.md) |
| ハマったことの記録 | `../技術メモ.md`（1つ上の階層） |

> 💡 「TODO教えて」と聞かれたら、Claude Code 内部の作業リストではなく
> **`TODO.md` のこと**。ファイルを読んで答える。

---

## 🧑‍🤝‍🧑 開発体制

| 役割 | 担当 |
|---|---|
| 🧒 **共同開発者** | 実装メイン。ゲームの中身（カード効果・バランス・見た目・遊びやすさ） |
| 👨 **オグさん** | 基盤担当。LAN対戦などの技術面、開発環境、**git操作** |

- **共同開発者は子供で、gitを直接触らない方針**。事故防止のため。
  git 操作が必要な場面では、共同開発者にコマンドを実行させず、オグさんに任せる。
- ゲームの内容は「実際に遊ぶ人の感覚」がいちばん正しいので、共同開発者の意見を尊重する。

---

## ⚠️ このプロジェクト特有の注意

### Googleドライブの共有フォルダ上にある

**2人が同時に同じファイルを編集すると、片方の変更が消える**（Driveが後勝ちで上書きする。gitでは防げない）。

- 作業を始める前に、`git status` で**他の人の作業途中の変更が無いか**確認する
- **他の人の未コミットの変更があるときに `git add -A` しない。**
  巻き込んでコミットしてしまう。必要なら**自分が触ったファイルだけ**を指定してコミットする

### 環境（ハマりどころ）

| 項目 | ルール |
|---|---|
| Python | **`py -3` を使う**。`python` はWindowsストアのダミーに繋がる |
| 依存 | **Flask のみ**（インストール済み） |
| git | 単体未インストール。GitHub Desktop 同梱のものを `tools/_git.ps1` が探す |
| `.ps1` | **UTF-8（BOM付き）で保存**。BOM無しだと日本語が構文を壊す |
| `.bat` | **中身はASCIIのみ。日本語を1文字も書かない**。`chcp` 後にcmdが読み位置を見失う |
| 文字コード | それ以外はすべて UTF-8 |

詳しい症状と原因は `../技術メモ.md` に記録してある。**新しくハマったら、そこに追記する**（症状→原因→対処の3点セット）。

---

## 🏗️ 設計の約束ごと

```
engine/    ゲームのルール本体。UIを一切知らない
server/    Flask。HTTPの入り口だけ。ルールを書かない
web/       ブラウザ画面。ルール判定をしない（できる操作はサーバーが actions で渡す）
```

- **数値は `balance.json` に置く。** コードに直書きしない
- **相手の手札は `Game.view(seat)` が落とす。** 画面側で隠すのではなくサーバー側で落とす（通信を覗かれても見えないように）
- **スマホ対応は `@media` の中だけに書く。** 外に書くとPC版が壊れる
  - `@media (max-width: 820px)` … 狭い画面
  - `@media (hover: none)` … マウスが無い端末
  - `@media (min-width: 1100px)` … 横長のPC
- CPU対戦もLAN対戦も「部屋(room)」として同じ仕組みで動く（`server/rooms.py`）

---

## 🛠️ よく使うコマンド

```
py -3 run.py                    # このPCだけで起動（localhost:5000）
py -3 run.py --lan              # スマホ・他PCからも繋がる（ポート80 → http://wish/ ）
py -3 simulate.py --games 400   # CPU同士で対戦してバランス確認
py -3 tools\gen_cardlist.py     # カードリストmdを再生成
```

ダブルクリック用：`ひとりで遊ぶ.bat` / `みんなで遊ぶ（スマホもOK）.bat`

### 変更したあと

| 直したファイル | 必要なこと |
|---|---|
| `server/` `engine/` `run.py` `balance.json` | **サーバー再起動** |
| `web/` の html・css・js | **ブラウザのリロードだけ**（キャッシュは無効化済み） |

`balance.json` を変えたら `py -3 simulate.py --games 400` で影響を確認する。
目安は **先攻勝率45〜55%／合計30〜40手番**。

---

## ✅ 作業のしめかた

1. 動作を確認する（サーバーを起動して実際に動かす。テストを書いたなら流す）
2. `TODO.md` の該当項目を `- [x]` にする
3. **機能を実装したときは** `バックアップ.bat` でzipスナップショットを取る
   （バグ修正だけのときは取らない。zip 1個＝動く状態のマイルストーン）
4. コミットは**オグさんの担当**。共同開発者に git コマンドを打たせない
